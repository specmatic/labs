version: 3

dependencies:
  services:
    - service:
        description: Service using camelCase contract
        definitions:
          - definition:
              source:
                filesystem:
                  directory: '.'
              specs:
                - camelCase.yaml
        runOptions:
          openapi:
            type: mock
            baseUrl: http://0.0.0.0:9090