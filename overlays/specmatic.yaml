version: 3

systemUnderTest:
  service:
    definitions:
      - definition:
          source:
            filesystem:
              directory: ./specs
          specs:
            - my-service.yaml
    runOptions:
      openapi:
        type: test
        baseUrl: http://myservice:8080
        # Step 2 of the lab: uncomment this line after updating the overlay file.
        # overlayFilePath: ./overlays/path-prefix.overlay.yaml

specmatic:
  license:
    path: /specmatic/specmatic-license.txt